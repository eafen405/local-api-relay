# Local API Relay

作为可由客户端自行选择的本地 API 供应商，对外发布模型，并在多个上游 API 供应商之间按成本与可用性路由请求。

## Language

**本地中转**:
对客户端表现为一个可自定义接入的 API 供应商，对外发布模型，但不管理客户端工具的配置。
_Avoid_: 工具管理器、配置切换器

**上游供应商**:
一份可由本地中转直接调用的上游连接配置，包含名称、Base URL 和一个上游 API key；不同配置可以使用相同的 Base URL。
_Avoid_: 供应商、渠道、节点

**发布模型**:
本地中转对客户端公开的逻辑模型身份，可由一条或多条代表同一模型身份的模型路由提供服务；启用前必须存在完整映射。成本排序与 Fallback 不得跨模型身份替换实际模型。
_Avoid_: 上游模型名、工具模型

**模型映射**:
模型路由中从发布模型名到上游模型名的显式对应关系；它是路由配置的一部分，不是独立对象。缺失时立即报错，不猜测调用用途或沿用其他模型。
_Avoid_: Prompt 识别、隐式继承、自动角色判断

同一发布模型的多条模型路由由管理员显式归组；相同模型身份的上游通常使用相同的上游模型名，本地中转不对不同名称做语义等价推断。

**模型选择**:
调用方从本地中转公开的发布模型中显式选定一个模型名；启动 agent 时由人或 harness 选择，agent 派发 subagent 时可再次选择。本地中转不理解 main、subagent、review 等用途，只在选定的发布模型内部选择模型路由。
_Avoid_: 用途映射、角色别名、中转代选模型

**模型路由**:
一个上游供应商与其提供的某个模型组成的可选择上游；可用性隔离发生在这一粒度。
_Avoid_: 上游供应商状态、模型状态

**中转访问密钥**:
本地中转签发给客户端的调用凭据；它按发布模型限定可使用的模型路由，不授予管理权限。
_Avoid_: 上游 API key、管理员凭据

**可用模型路由集**:
一个中转访问密钥对某个发布模型获准使用的模型路由子集，也是该密钥成本排序与 Fallback 的边界。
_Avoid_: 上游白名单、路由优先级

**管理员凭据**:
单个本地管理员访问管理面的凭据，与客户端调用所用的中转访问密钥相互独立。
_Avoid_: 中转访问密钥、用户账号

**上游 API key**:
本地中转代表用户调用上游供应商时使用的秘密凭据，不是客户端访问本地中转的凭据。
_Avoid_: 中转访问密钥、管理员凭据

**成本倍率**:
用户为同一发布模型的模型路由填写的相对成本；以 `1x` 为基准，倍率越低越优先。
_Avoid_: 绝对价格、自动报价、套餐价格、手动优先级

**缓存命中率**:
缓存输入 token 占输入 token 总数的比例；输入 token 总数等于未缓存输入与缓存输入之和。上游未返回缓存 token 时按零缓存输入计算。
_Avoid_: 从费用金额反推缓存、把缓存命中率作为路由选择因素

**暂不可用**:
模型路由因一次可归因于上游的故障而被候选集排除；旧的在途请求不能恢复它，只有当前隔离周期的恢复检测成功才能使其重新可用。
_Avoid_: 供应商下线、永久禁用

**检测中**:
模型路由尚未完成启动检测、因此不能进入候选集的状态；检测成功后可用，失败后暂不可用。
_Avoid_: 默认健康、沿用上次健康状态

**恢复检测**:
暂不可用的模型路由为证明目标模型、凭据、端点与原生协议已经恢复而执行的专用最小请求；健康路由不接受周期性恢复检测。
_Avoid_: 网络连通性检查、模型列表检查、旧请求成功

**Fallback**:
当前模型路由失败后，将请求转交给同一模型的下一条可用路由。
_Avoid_: 模型降级、随机重试

**调用记录**:
一次客户端调用的 metadata-only 统计摘要，显示调用时间、发布模型，并在发布模型下显示最终实际承接响应的上游供应商；同时显示该成功模型路由尝试报告的总 token（其中缓存 token）、估算费用和响应完成耗时，流式调用还显示首字耗时。此前失败的模型路由尝试不计入调用记录的 token 与费用。即使发生 Fallback，一次客户端调用仍只有一条调用记录，且绝不包含请求或响应的内部内容。全部模型路由尝试均失败时仍保留调用记录，明确显示没有成功上游，并以未知值而非零表示 token、费用和耗时；失败记录不进入 token 与费用聚合。
_Avoid_: 请求内容、响应内容、逐路由调用记录

**模型路由尝试**:
一次调用在某条模型路由上的单次尝试。Fallback 发生时，调用记录可以展开模型路由尝试链，以顺序、耗时、HTTP 状态或规范化错误类别说明故障与转交过程，但不暴露请求或响应的内部内容。
_Avoid_: 独立调用记录、请求内容、原始上游错误正文
