# 09 — 个人中转密钥简化：明文持久化 + 可再次显示

**What to build:** REL-010。`relay_access_keys.secret` 明文列（owner-only 文件权限不变）；创建后与列表中均可显示完整密钥（复制按钮）；移除"仅一次"限制；旧哈希密钥显示"旧版本创建，请重新创建"。用户指令：个人中转站，安全性不在意，砍掉此处过度设计。

**Blocked by:** None — 可独立收尾。

**Status:** resolved

- [ ] store：列迁移 + 创建/读取明文。
- [ ] server：列表/详情返回完整密钥字段。
- [ ] web：密钥行"显示"按钮。
- [ ] 测试：修订"仅一次/仅哈希"既有断言；明文可再取回。

Spec coverage: `REL-010`（修订 SEC-006）。

